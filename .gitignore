venv/
tmp/

# dependencies
node_modules/

# build output (main.js ships as a GitHub release asset, not in git)
main.js
main.js.map
dist/

# test / gate output
coverage/
reports/
artifacts/
*.tsbuildinfo

# throwaway projects built by tests/build/*.test.ts
coverage-gate-fixture/
reporter-contract-fixture/

# credentials never belong in this repo (check:secrets scans for them too)
.env
.env.*

