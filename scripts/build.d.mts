// Types for the build script's exported constants, so tests can import the
// build's own allow-list instead of re-deriving it (tests/build/artifact-metafile.test.ts).
export declare const EXTERNAL: string[];
